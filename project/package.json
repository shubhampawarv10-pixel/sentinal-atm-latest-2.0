{
  "name": "project",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd sentinal-atm-main && npx vite --port 3000 --host",
    "build": "cd sentinal-atm-main && npx tsc -b && npx vite build",
    "preview": "cd sentinal-atm-main && npx vite preview"
  },
  "dependencies": {},
  "devDependencies": {}
}
